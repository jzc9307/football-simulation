// Crest files live in src/assets/club-logos as eu-<club id>.png. UI components
// resolve those bundled assets with import.meta.glob; keeping this game module
// data-only means career and calendar tests can run in plain Node as well.
export const GUEST_CRESTS={};
